[package]
name = "basalt-db"
version = "0.1.6"
edition = "2024"
rust-version = "1.88"
description = "CLI-first local SQL workspaces for structured data and coding agents"
license = "MIT"
readme = "README.md"
repository = "https://github.com/joshiii-xyz/basalt"
keywords = ["agents", "cli", "mcp", "sql", "workspace"]
categories = ["database-implementations", "command-line-utilities"]
exclude = [".github/", "dist-workspace.toml", "qa/"]

[lib]
name = "basalt"
path = "src/lib.rs"

[[bin]]
name = "basalt"
path = "src/main.rs"

[dependencies]
csv = "1.3"
fs4 = "1.1.0"
futures = "0.3"
rmcp = { version = "3.2.0", features = ["elicitation", "macros", "request-state", "schemars", "server", "transport-io"] }
schemars = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.11"
tokio = { version = "1", features = ["io-std", "net", "rt-multi-thread", "sync", "time"] }
tokio-util = { version = "0.7", features = ["codec"] }
tracing = "0.1"
uuid = { version = "1", features = ["v4"] }

[[bench]]
name = "throughput"
harness = false

[profile.release]
opt-level = 3
lto = true
strip = true

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
