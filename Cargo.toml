[package]
name = "basalt"
version = "0.1.0"
edition = "2024"
description = "An embedded SQL database engine built from scratch: parser, planner, MVCC transactions, WAL, B-tree indexes"
license = "MIT"

[dependencies]

[profile.release]
opt-level = 3
lto = true
strip = true
